---
phase: 07-refactor-cmux-notify
plan: "01"
task: 0
total_tasks: 2
status: not_started
last_updated: 2026-03-20T20:24:56.560Z
---

<current_state>
Phase 7 plan created (07-01-PLAN.md), nothing implemented yet.
Ready to start task 1: write cmux-notify.test.ts (TDD red phase).
</current_state>

<completed_work>
- Plan created: 07-01-PLAN.md — full TDD plan with must_haves, dispatch map contract, behavior tests spec
- Design decision locked: class-based + dispatch map, no FSM library (see decisions below)
</completed_work>

<remaining_work>
- Task 1: Write src/cmux-notify.test.ts — 10 behavior tests covering:
  - dispatch map routing (one test per event type)
  - session.status busy/idle sidebar transitions
  - pending-input guard (isWaitingForInput blocks done notify)
  - permission.ask hook eager registration
  - tui.prompt.append hinting (fires once)
  - Run vitest → confirm all red (TDD red phase)
- Task 2: Implement CmuxNotifyPlugin class extending PluginBase:
  - All closure state → class instance fields (pendingPermissions, pendingQuestions, sessionBusy, hinted, hint)
  - Event handler → dispatch map: Record<EventType, handler method>
  - Factory export CmuxPlugin unchanged
  - Run vitest → green; typecheck → clean; build → clean
</remaining_work>

<decisions_made>
- No FSM library (typescript-fsm): pending-input tracking uses Sets (multiple concurrent permissions/questions), which a strict FSM can't model cleanly. Named-method dispatch map gives the same readability win with zero new deps.
- TDD: cmux-notify has zero existing tests; write tests first to lock in behavioral contract before touching implementation.
- Mirror cmux-subagent-viewer.ts pattern exactly: class extending PluginBase, constructor receives ctx, hooks() method returns the plugin hooks object.
</decisions_made>

<blockers>
None.
</blockers>

<context>
User asked for class-based refactor of cmux-notify (currently closure-based) to match cmux-subagent-viewer.ts structure. Also wanted the large if-chain event handler cleaned up — evaluated typescript-fsm library but decided against it (pending-input sets don't map cleanly to a single FSM state). The plan uses a dispatch map pattern instead. 07-01-PLAN.md is fully written with concrete implementation guidance including the class skeleton and dispatch map structure.
</context>

<next_action>
Start with: Read src/cmux-notify.ts and src/lib/plugin-base.ts to confirm field names, then write src/cmux-notify.test.ts with 10 behavior tests. Run `npm test` to confirm red. Then implement the class.
Run `/gsd-execute-phase 7` after `/clear`.
</next_action>
