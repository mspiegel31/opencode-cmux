---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BUGFIX]

must_haves:
  truths:
    - "Sidebar colors correctly reflect task state (working, need prompt)"
    - "Root cause for color bug identified"
  artifacts: []
  key_links: []
---

<objective>
Investigate and debug why the cmux sidebar is not changing colors according to the task state (working, need prompt, etc).

Purpose: The sidebar should provide a visual cue to the user regarding the current state of the OpenCode agent. If colors are not updating, this functionality is broken.
Output: Identified root cause, debug logs, and a fix ensuring color updates work correctly.
</objective>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate and analyze sidebar color logic</name>
  <files>src/*</files>
  <action>
    Use the `glob` and `grep` tools to search the `src/` directory (e.g., for "color", "working", "need prompt", "sidebar", "pane") and find the component or function responsible for updating the cmux sidebar color based on task state. Identify the expected event flow (e.g., listening for `session.status`, `permission.ask`, `question.asked`).
  </action>
  <verify>
    <automated>grep -rl "color" src/ || true</automated>
  </verify>
  <done>Relevant code handling sidebar color changes is located and the expected logic is understood.</done>
</task>

<task type="auto">
  <name>Task 2: Add debugging and identify root cause</name>
  <files>src/*</files>
  <action>
    Add debug logging (using `console.log` or writing to a debug file) to the event handlers identified in Task 1. Check if the relevant events are firing and if the logic for determining the new color and updating the TUI is executing. Use the `bash` tool to build and run a quick test or unit test to observe the logs. Determine if the issue is missing events, a logic bug, or incorrect TUI API usage.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Root cause of the sidebar color bug is documented via debug output.</done>
</task>

<task type="auto">
  <name>Task 3: Apply fix and verify</name>
  <files>src/*</files>
  <action>
    Implement the fix based on the findings from Task 2. Run the test suite (`npm run test`) to ensure the fix is correct and no regressions were introduced.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Sidebar color change logic works as expected and tests pass.</done>
</task>

</tasks>

<verification>
Ensure the cmux TUI pane updates its style/color properties according to agent states such as "working", "need prompt", etc.
</verification>

<success_criteria>
Bug is fixed, colors update correctly, and all tests pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260320-s9a-debug-cmux-sidebar-color-changes-accordi/260320-s9a-SUMMARY.md`
</output>