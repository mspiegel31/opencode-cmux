# Task 11 Evidence: CmuxBrowserToolsPlugin Unit Tests

## File Created
src/cmux-browser-tools.test.ts

## Test Count
16 tests across 6 describe blocks (well above the ≥10 requirement)

## Test Suite Results
Test Files: 3 passed (3)
Tests:      47 passed (47)

## Test Categories
1. Plugin structure (2 tests)
   - instantiation with mock PluginInput
   - hooks() returns object with tool property

2. Tool registration (4 tests)
   - navigation tools (browser_navigate, browser_snapshot, browser_screenshot, browser_find, browser_url)
   - DOM interaction tools (browser_click, browser_fill, browser_type, browser_hover, browser_scroll)
   - page management tools (browser_new_page, browser_wait_for, browser_handle_dialog, browser_list_pages)
   - utility tools (browser_evaluate, browser_console_messages, browser_network_requests, browser_emulate, browser_get, browser_is)

3. Guard behavior (2 tests)
   - factory returns {} when CMUX_WORKSPACE_ID not set
   - factory returns {} when cmuxBrowserTools.enabled is false (via CMUX_BROWSER_TOOLS_ENABLED=false env)

4. Command construction (5 tests)
   - browser_navigate (url): verifies goto + --snapshot-after + --surface
   - browser_click: verifies click + selector + --snapshot-after + --surface
   - browser_snapshot: verifies snapshot + --surface
   - browser_evaluate: verifies eval + --script + --surface
   - browser_navigate (back): verifies back + --snapshot-after

5. Surface management (2 tests)
   - surface ID cached: browser open called only once for two consecutive tool calls
   - dead surface detection: quiet() probe throws → browser open called again (≥2 times)

6. Error handling (1 test)
   - shell throws → execute() returns string matching /^Error:/

## Counterfactual Test
Broke assertion in "browser_navigate (url)" test:
  CHANGED: expect(navCall).toContain("--snapshot-after")
  TO:      expect(navCall).toContain("--nonexistent-flag")

Result: Test FAILED with:
  AssertionError: expected 'cmux browser goto https://example.com --snapshot-after --surface surface:42'
  to contain '--nonexistent-flag'

After revert: 47/47 tests pass ✓

## Key Implementation Notes
- makeMockCtx() uses response-key matching: the first shellResponses key found in the command string returns that value
- reconstructCmd() interleaves TemplateStringsArray parts with interpolated values (including array-flattening for Bun shell flags)
- Guard tests manipulate env vars with try/finally cleanup
- Surface management tests use a custom inline proxy for fine-grained control over probe vs. open behavior
