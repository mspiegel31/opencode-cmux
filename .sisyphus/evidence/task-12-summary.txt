Task 12: Focus-gating tests — evidence summary
================================================

Added describe block: "CmuxNotifyPlugin — focus-gating"
File: src/cmux-notify.test.ts

5 new test cases (Tests 11-15):
  Test 11: Focused + onlyWhenUnfocused=true → notification suppressed, sidebar still updates
  Test 12: Not focused + onlyWhenUnfocused=true → notification fires
  Test 13: Focused + onlyWhenUnfocused=false → notification fires regardless
  Test 14: Sidebar NOT suppressed when focused (busy → set-status Working)
  Test 15: Focus detection failure → fail-open (notification fires)

Mock strategy:
  - isFocused variable at describe scope (reset in beforeEach)
  - throwOnIdentify variable at describe scope (reset in beforeEach)
  - Proxy on ctx.$ checks cmdStr.includes("identify") to route focus detection
  - Focused: { caller: { surface_ref: "surface:1" }, focused: { surface_ref: "surface:1" } }
  - Unfocused: { caller: { surface_ref: "surface:1" }, focused: { surface_ref: "surface:99" } }
  - Fail: text() rejects with Error → isSurfaceFocused() catch returns false → notification fires

Test results:
  Final: 31/31 passed (26 existing + 5 new)
  Counterfactual: Test 12 with .toBe(false) → failed "expected true to be false" ✓
